"""
URL configuration for SYRA store app.
"""

from django.urls import path
from apps.store import views

app_name = 'store'

urlpatterns = [
    # Store endpoints (ViewSet routes handled in main urls.py)
]